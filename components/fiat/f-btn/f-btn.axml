<import-sjs name="element" from="../../../utils/element.sjs"/>

<button 
  class="{{ element.classAttr(defaultClasses, className) }}"
  onTap="onTap"
  disabled="{{disabled}}"
>
  <slot></slot>
</button>