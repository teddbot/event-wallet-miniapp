<import-sjs name="element" from="../../../utils/element.sjs"/>

<view class="{{ element.classAttr('f-overlay f-overlay--' + type, className) }}" onTap="onTap">
  <view a:if="{{ type == 'blur' }}" class="background"/>
  <slot/>
</view>