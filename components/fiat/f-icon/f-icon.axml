<import-sjs name="element" from="../../../utils/element.sjs"/>

<view class="{{ element.classAttr('f-icon', className) }}"
  style="background-image: url('components/fiat/assets/{{ name }}.svg'); {{ w && 'width:' + w + 'px' }}; {{ h && 'height:' + h + 'px' }};"
  onTap="onTap">
  <slot/>
</view>