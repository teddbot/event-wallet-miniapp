<import-sjs name="element" from="../../../utils/element.sjs"/>

<view class="{{ element.classAttr('f-divider ' + type + (type === 'title' ? ' f-caption caption-10 secondary' : ''), className) }}" onTap="onTap">
  {{ title }}
</view>